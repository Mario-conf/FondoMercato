import type { Transaction } from '@/lib/types';

// This file is now used for type definitions and initial structure,
// but the data is managed in the DataProvider context using localStorage.
export const transactions: Transaction[] = [];
